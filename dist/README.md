# Build output

Compiler-specific Pascal units, objects, executables, and native libraries are
written to `build/` or `lib/` and are intentionally excluded from the npm
package. This directory is reserved for future portable distribution assets.
